"use client";

import { WriterPromptEditor } from "@/components/write/writer-prompt-editor";

/** Prompt workspace create — Writer Prompt Editor (Prompt Lab API). */
export function PromptEditor() {
  return <WriterPromptEditor variant="admin" />;
}
